<!DOCTYPE html>
<html lang="en"><head>
    <meta http-equiv="content-type" content="text/html">
    <meta charset="UTF-8">
    <meta name = "viewport" content="width = device- width, initial-scale=1.0">
    <meta http-equiv = "X-AU-Compatible" content="ie=edge">
    <title>Вход для администраторов</title>
    <link rel="stylesheet" type="text/css"  href="">
    <link rel="stylesheet" href="../css/rPageStyles.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<body>
<div id="wrapper">
    <table border="1">
        <tr>
            <th>usersData.fName</th>
            <th>usersData.sName</th>
            <th>usersData.position</th>
            <th>usersData.theme</th>
        </tr>
    </table>
    <% for(let i = 0; i < usersData.questResults.length; i++) {%>
    <p>Вопрос <%=i+1%></p><br>
    <fieldset>
        <legend><%=usersData.questResults[i].content1%></legend>
        <pre><code><%=usersData.questResults[i].content2%></code></pre><br>
        <% for(let j = 0; j < usersData.questResults[i].arrAnswers.length; j++) {%>
            <input type="radio" required name="answer" id="r<%= j+1%>" disabled = disabled><span style = " background-color:<% if(data.questResults[i].answerIdArr[j] == data.questResults[i].idRightAnswer){%> darkseagreen<%}else if(data.questResults[i].idAnswer == data.questResults[i].answerIdArr[j]){%>red<%}%>"><%= usersData.questResults[i].arrAnswers[j]%></span><br>
        <%} %>
    </fieldset>
    <%} %>
</div>
</body>
</html>